# Frontend (Netlify). Not used yet — the admin panel runs on mock data.
# Anything prefixed VITE_ is visible in the browser, so never put secrets here.

VITE_API_URL=https://api.zabacco.co.tz/api
VITE_CLOUDINARY_CLOUD_NAME=zabacco
VITE_CLOUDINARY_UPLOAD_PRESET=zabacco_unsigned
